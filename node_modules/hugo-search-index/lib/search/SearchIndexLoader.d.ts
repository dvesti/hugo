import { Index, Options, SearchIndexLib } from 'search-index';
export declare class SearchIndexLoader {
    private lib;
    private options;
    constructor(lib?: SearchIndexLib, options?: Options);
    load(contents: Uint8Array, cb: (err: Error, index?: Index) => void): void;
}
export default SearchIndexLoader;
